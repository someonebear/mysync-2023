#include "mysync.h"

// Create all missing directories in path.
void create_dirs(char *path);

void sync_files();

void find_difference(int num_dir)
{
  printf("%s\n", top_directories[0]);
  printf("%s\n", keys[0]);
}
